---
# ============================================================
# Secret variables — encrypt this file with ansible-vault:
#   ansible-vault encrypt ansible/vars/secret_vars.yml
#
# To edit later:
#   ansible-vault edit ansible/vars/secret_vars.yml
# ============================================================

# Example secrets (replace with real values before encrypting):
# docker_registry_user: myuser
# docker_registry_password: mypassword
